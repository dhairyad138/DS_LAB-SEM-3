#include<stdio.h>
int binary_search(int arr, int left, int right)
{
if(left > right)
{
    return -1
}
int middle = (left+right)/2
if(key == arr(middle))
{
    return middle+1
}
else if()
}
